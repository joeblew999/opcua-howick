# Email / LINE to Erick — Maxxi Factory

Hi Erick,

I'm working on a system that delivers job files to roll-forming machines
automatically — no more USB sticks. I've already got it working on a
Howick FRAMA, and I'd like to support FrameCad machines too.

To get started I need a few things from your site. Most of it takes
five minutes:

**The main thing I need:**

1. **A complete job folder** from your FrameCad machine — the same files
   your operator puts on the USB stick. All the files, not just one —
   I need to see the `.rfy` file (this is what the machine actually reads),
   plus any `.csv`, `.xml`, or `.nexa` files that go with it. Two different
   jobs would be ideal (e.g. a wall frame and a truss).

**Also helpful if you have a minute:**

2. **Machine model** — a photo of the nameplate on the machine
   (e.g. F325iT, F450iT)
3. **How do jobs get to the machine today?** — what software do you
   use to design, and how does the file end up on the USB stick?
4. **Are you using FrameCad Nexa?** — and if so which tier?
5. **A photo of the USB port area** on the machine

No rush — whenever you get a chance. You can send the files by email
or LINE, whatever is easier.

Cheers,
Gerard
