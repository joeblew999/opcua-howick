[toolchain]
channel = "1.93.0"
components = ["rustfmt", "clippy"]
# Cross-compilation targets added via: rustup target add <target>
# aarch64-unknown-linux-gnu   (Raspberry Pi 5)
# armv7-unknown-linux-gnueabihf (Raspberry Pi 3/4)
# x86_64-unknown-linux-gnu    (Linux server)
